{%  extends 'base.html.twig' %}

{% block title %}
    Liste des figures
{% endblock %}

{% block body%}
        <div class="container mb-5 mt-4 " id="section">
            <div class="bg-light ">
                <div class="row">
                    <div class="col-12 col-md-8"><h1 class="p-2">Les figures</h1></div>
                    <div class=" col-12 col-md-4 d-flex align-items-center justify-content-end "><a href="#" type="button" class="btn btn-sm btn-outline-secondary mr-3">Ajouter une figure</a></div>
                </div>

                <hr>
                {% for message in app.flashes('success') %}
                    <div class="alert alert-success text-center" role="alert">
                        {{ message }}
                    </div>
                {% endfor %}
                <div class="row p-2">
                    {% for  trick in tricks %}
                        <div class="col-12  col-lg-3">
                            <div class="card mb-4 shadow-sm">
<<<<<<< HEAD
                                {% for media in trick.medias %}
                                    {% if media.header == 1 %}<img src="{{ media.path }}" alt=" image trick : {{ trick.name }}" class="img-fluid"/>{% endif %}
=======
                                {% for media in medias %}
                                    {% if trick == media.trick %}<img src="{{ media.path }}" alt="{{ media.path }}" class="img-fluid"/>{% endif %}
>>>>>>> a8eda677f29c4ea275ca8c28a4711d6ba96277a8
                                {% endfor %}
                                <div class="card-body d-flex justify-content-between">
                                    <a href="{{ path('trick.show', {id: trick.id})}}" style="font-weight: bold;color: black;">{{ trick.name }}</a>
                                    <div>
                                        <a href="#"><i class="icon fa fa-pencil"></i></a>
                                        <form method="post"  style="display:inline-block" onsubmit="return confirm('Etes vous sure de vouloir supprimer?')">
                                            <input type="hidden" name="_method" value="DELETE">
                                            <input type="hidden" name="_token" value="{{ csrf_token('delete') }}">
                                            <button type="submit" class="icon fa fa-trash-o " style="background: none;border:none; cursor: pointer"></button>
                                        </form>
                                    </div>
                                </div>
                            </div>
                        </div>
                    {% endfor %}
                </div>
                <div class="col-12 text-right ">
                    <a href="#carouselExampleIndicators" ><i class="icon fa fa-arrow-up m-3" style="font-size: 40px"></i></a>
                </div>
            </div>
        </div>
{% endblock %}