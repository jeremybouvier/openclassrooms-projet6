{{ form_start(form) }}

    <div class="col-12 col-lg-10 offset-lg-1">
        <div class="row">
            <div class="col-12 form-group" >{{ form_row(form.message) }}</div>
        </div>
    </div>

{{ form_widget(form) }}

    <div class="col-12 col-lg-6 offset-lg-3 d-flex mb-5 justify-content-center">
        <button class="btn btn-sm btn-outline-info col-6">Poster le message</button>
    </div>

{{ form_end(form) }}